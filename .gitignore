# Paquetes generados para subir a hostings (se regeneran)
*.zip

# Configuración local del editor/entorno
.claude/

# Basura del sistema
Thumbs.db
ehthumbs.db
Desktop.ini
.DS_Store

# Copia local de las fotos en alta (no hace falta versionarla)
_originales/

# Locks de coordinacion entre agentes (son temporales, no se versionan)
.agentes/locks/*.lock
